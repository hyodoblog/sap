= render( partial: 'sap/shared/sessions',
          locals: { title: '学生専用ログイン',
                    admin: @admin,
                    model: resource,
                    url: session_path(resource_name),
                    password_link: new_student_password_path(@sap_key) } )